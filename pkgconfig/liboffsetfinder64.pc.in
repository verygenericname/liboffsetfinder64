prefix=PREFIX
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${prefix}/include

Name: liboffsetfinder64
Description: an offsetfinder for 64bit ARM

Requires: libimg4tool >= 192 libgeneral >= 39 libinsn >= 24
Version: VERSION_COUNT
Libs: -L${libdir} -lgeneral -loffsetfinder64 -linsn
Cflags: -I${includedir}
